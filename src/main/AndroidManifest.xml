<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android_advertisement_app">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label=""
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Android_Advertisement_APP">
        <activity android:name=".UserUpdateShowList"></activity>
        <activity android:name=".UpdateAd" />
        <activity android:name=".Login"></activity>
        <activity android:name=".DetailsOfSelectedAd" />
        <activity android:name=".Registration"></activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".AddRecords" />
        <activity android:name=".MainPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>